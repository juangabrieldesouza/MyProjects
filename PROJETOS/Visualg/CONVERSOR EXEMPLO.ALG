algoritmo "CONVERSOR EXEMPLO"
var
   Angulo, s: REAL
inicio
      Escreva("Informe um Ângulo: ")
      Leia (Angulo)
      s <- Sen(GraupRad(Angulo))
      Escreva("O seno de ", Angulo, " é igual a ", s)
fimalgoritmo